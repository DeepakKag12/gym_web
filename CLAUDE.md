# Project Instructions

## GitHub Account Constraint

This repository is `DeepakKag12/gym_web`.

Use only the GitHub account `DeepakKag12` (`kagdeepak45@gmail.com`) for this project. This applies to any commands or instructions involving GitHub CLI, Git operations, repository access, pull requests, pushes, and authentication-related actions.

Before account-sensitive operations, verify that `DeepakKag12` is the active account with:

```sh
gh auth status --hostname github.com
```

Do not switch to, use, or publish through another GitHub account for this repository.

## User-Executed Commands Only

The user has final control over all command execution. Do not execute commands that write to the local repository or remote repository, including commits, pushes, pull requests, merges, releases, branch changes, file edits, dependency installs, or other mutations. Do not execute GitHub CLI write commands.

For every required command, provide the exact command followed by a concise explanation of its meaning and purpose. The user will execute it locally or remotely. Read-only inspection commands may be run only when the user has not reserved their execution, but prefer providing commands for the user to execute when practical.

## Maintaining This File

When a new project-level rule or durable workflow decision is established, record it in this file so future Claude sessions inherit it. Keep rules concise, specific, and limited to this repository.
